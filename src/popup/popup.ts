function bookmark() {
  let background = chrome.extension.getBackgroundPage();
  // let chromeBookmark = background.chromeBookmark;
  // let revengeBookmark = background.revengeBookmark;

  // if (revengeBookmark) {
  //   document.writeln(revengeBookmark.url);
  // }
}

window.onload = bookmark;
